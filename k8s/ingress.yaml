apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stateless-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: public
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: stateless-app-service
            port:
              number: 80